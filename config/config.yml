bind_addr = ":8080"
log_level = "debug"
database_url = "host=localhost user=admin password=admin dbname=postgres_db sslmode=disable"

port: "8080"

db:
  username: "postgres"
  host: "localhost"
  port: "5432"
  dbname: "postgres"
  sslmode: "disable"